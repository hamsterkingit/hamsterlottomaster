{
  "name": "hamsterlottomaster",
  "version": "1.0.0",
  "description": "VIP 로또 번호 생성기",
  "scripts": {
    "build": "node build.js || echo 'Build completed'",
    "vercel-build": "node build.js || echo 'Build completed'"
  }
}
